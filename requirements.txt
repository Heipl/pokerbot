# Minimal runtime + training deps (pip alternative to environment.yml).
#   python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# CPU-only. torch is intentionally absent: only neural_cfr_bot needs it, and that
# bot is experimental. Install it separately if you work on it.
cython>=3.2.3
pkrbot>=1.1.0
numpy>=1.24
